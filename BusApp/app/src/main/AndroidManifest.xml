<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/adityabusround"
        android:roundIcon="@mipmap/adityabusround"
        android:label="@string/app_name"
        android:theme="@style/Theme.BusApp">

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyB7R0wS4K1wqdjkNVve2UMyI1EVlWCOFfs" />

        <activity android:name=".StudentDashboardActivity" />
        <activity android:name=".DriverDashboardActivity" />
        <activity android:name=".StudentRegistrationActivity" />
        <activity android:name=".DriverRegistrationActivity" />
        <activity android:name=".StudentLoginActivity" />
        <activity android:name=".DriverLoginActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
